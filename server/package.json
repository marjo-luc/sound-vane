{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean-start": "npm run clean && npm i && node server.js",
    "start": "node server.js",
    "clean": "rm package-lock.json && rm -rf node_modules"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "dotenv": "^16.0.2",
    "express": "^4.17.2"
  }
}
